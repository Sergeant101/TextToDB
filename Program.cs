using System;

namespace TextToDB
{
    class Program
    {


        class PrepareData
        {

        }

        static void Main(string[] args)
        {

            // Путь к файлу
            string Path = null;

            // Диалоговое окно
            StartDialogs begginTxtToDB = new StartDialogs();

            begginTxtToDB.Hello();
            begginTxtToDB.Check();


            // =========================================================================================================
            // тестирование удалить
            string test = null;

            
            // =========================================================================================================
            // Тестирование удалить
            /*
            if (checkFile.ExistOK)
            {
                test = "  OK";
            }
            else
            {
                test = "  Wrong";
            }
         */
            // =========================================================================================================
            // тестирование удалить

            // =========================================================================================================
            // тестирование удалить
            //Console.WriteLine("Hello World!" + test + "  " + Convert.ToString(checkFile.testSizeFile) + "  " + checkFile.testBOM[0] + "  " + checkFile.testBOM[1] + "  " + checkFile.testBOM[2]);

            // =========================================================================================================
            // Приветствие
            

        }
    }
}
